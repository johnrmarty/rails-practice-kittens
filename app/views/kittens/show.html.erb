
<%= image_tag @kitten.image %> 

<h1>ADD A KITTEN CATEGORY</h1>

<%= form_for @kitten do |f| %>


<%= f.label :category_id %>
    <em><%=f.collection_select(:category_id, KittensCategories.all, :id, :name,{include_blank:"Select kitten category"})%></em>


  <%= f.submit %>

<%= link_to 'Cancel', root_path %>





<% end %>


